[package]
name = "api"
version = "0.1.0"
authors = ["rust-vmm AWS maintainers <rust-vmm-maintainers@amazon.com>"]
edition = "2018"

[dependencies]
clap = "4.5.28"

vmm = { path = "../vmm" }

[dev-dependencies]
linux-loader = "0.13.0"
